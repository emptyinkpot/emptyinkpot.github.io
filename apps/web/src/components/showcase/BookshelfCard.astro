---
import type { BookItem } from '../../lib/books/types';
import BookCover from './BookCover.astro';
import { withBase } from '../../lib/site';
import { getBookDetailHref } from '../../lib/books/routes';

interface Props {
  book: BookItem;
  compact?: boolean;
}

const { book, compact = false } = Astro.props;
const href = withBase(getBookDetailHref(book));
---

<a class:list={['showcase-card', compact && 'showcase-card--compact']} href={href}>
  <BookCover title={book.title} cover={book.cover} />
  <div>
    <span>书架 / {book.statusLabel}</span>
    <strong>{book.title}</strong>
    <small>{book.author}</small>
    <p>{book.note}</p>
  </div>
</a>
