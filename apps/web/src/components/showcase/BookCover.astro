---
interface Props {
  title: string;
  cover?: string;
}

const { title, cover } = Astro.props;
---

<div class="showcase-cover showcase-cover--book">
  {cover ? <img src={cover} alt={title} loading="lazy" /> : <span>{title.slice(0, 2)}</span>}
</div>
