# Astro主链路收口与Hexo去除改造计划书

日期：2026-04-02

## 文档定位

这份计划文档用于记录本轮仓库级收口改造的正式执行方案。

对应的公开发布版本以站内文章为准：

- `apps/web/src/content/posts/2026-04-02-Astro主链路收口与Hexo去除改造计划书.md`

本文件承担的职责是：

- 作为 `docs/plans/` 下的阶段计划记录
- 为后续治理、执行日志和回顾提供计划基线
- 保留本轮改造的执行目标、范围和验收标准

## 核心目标

1. 收口到 Astro 单一主链路
2. 移除 Hexo 历史运行链路
3. 统一内容、构建、部署和文档口径
4. 让仓库顶层目录更接近《通用文件与模块开发规范》

## 对应公开文章

为避免工程计划和公开内容双向漂移，本轮方案的公开版统一维护在：

- `apps/web/src/content/posts/2026-04-02-Astro主链路收口与Hexo去除改造计划书.md`

若本计划后续需要对外展示，应优先更新站内文章版本，再决定是否同步到本文件。

## 结果摘要

截至当前阶段，本轮计划已经完成以下目标：

1. 删除 Hexo 依赖、脚本和历史目录
2. 将正式站点入口收口到 `apps/web/`
3. 将更新日志真源收口到 `public-data/updates/index.md`
4. 补建 `tests/` 与 `.runtime/` 占位
5. 将根目录精简到正式职责目录

## 后续建议

1. 继续压缩 `docs/` 中残留的历史阶段描述
2. 为 `tools/` 做 `validate / generate / migrate` 分层
3. 逐步加强 `apps/web/` 内部边界治理
